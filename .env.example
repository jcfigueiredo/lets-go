# Documentation only. `make up` regenerates .env with the dynamic host port
# that Docker Compose assigns to the postgres service. The port below is
# illustrative — your actual port will differ on each `make up`.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/lab
TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/lab_test
